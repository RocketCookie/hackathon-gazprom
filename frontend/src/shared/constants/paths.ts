enum PATH {
  HOME = '/',
  LOGIN = '/login',
  REGISTER = '/register',
  STRUCTURE = '/structure',
  PLATFORM = '/platforms',
  PRODUCTS = '/products',
  SERVICE = '/services',
  TEAMS = '/teams',
  EMPLOYEES = '/employees',
}

export const paths = PATH;
